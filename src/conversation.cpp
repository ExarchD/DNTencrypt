#include <conversation.h> 
#include <iostream>
#include <string>
#include <vector>
using namespace std;

     int Conversation::send (vector<string> recipients, string msg)
        {
            cout << msg << endl;
        }
